--- a/CustomerServiceApp/RootView.swift
+++ b/CustomerServiceApp/RootView.swift
@@ -248,18 +248,49 @@
     }
 }
 
+struct MediaPreview: Identifiable, Hashable {
+    let id = UUID()
+    let url: URL
+    let kind: String
+}
+
 struct MessageContent: View {
     @EnvironmentObject var vm:ChatViewModel
     @AppStorage("csAppearance") private var appearance = "light"
+    @State private var preview: MediaPreview?
     let message:ChatMessage
     let isAgent:Bool
+
     var body: some View {
         Group {
             if message.kind == "image", let url=absoluteURL(message.content) {
-                AsyncImage(url:url) { image in image.resizable().scaledToFit() } placeholder:{ ProgressView().frame(width:150,height:120) }
-                    .frame(maxWidth:250,maxHeight:300).clipShape(RoundedRectangle(cornerRadius:12,style:.continuous))
+                Button { preview = MediaPreview(url:url, kind:"image") } label: {
+                    AsyncImage(url:url) { image in
+                        image.resizable().scaledToFit()
+                    } placeholder:{
+                        ProgressView().frame(width:150,height:120)
+                    }
+                    .frame(maxWidth:250,maxHeight:300)
+                    .clipShape(RoundedRectangle(cornerRadius:12,style:.continuous))
+                }
+                .buttonStyle(.plain)
+                .accessibilityLabel("打开图片预览")
             } else if message.kind == "video", let url=absoluteURL(message.content) {
-                VideoPlayer(player:AVPlayer(url:url)).frame(width:240,height:155).clipShape(RoundedRectangle(cornerRadius:12,style:.continuous))
+                ZStack(alignment:.topTrailing) {
+                    VideoPlayer(player:AVPlayer(url:url))
+                        .frame(width:240,height:155)
+                        .clipShape(RoundedRectangle(cornerRadius:12,style:.continuous))
+                    Button { preview = MediaPreview(url:url, kind:"video") } label: {
+                        Image(systemName:"arrow.up.left.and.arrow.down.right")
+                            .font(.system(size:13,weight:.bold))
+                            .foregroundStyle(.white)
+                            .frame(width:34,height:34)
+                            .background(.black.opacity(0.56),in:Circle())
+                    }
+                    .buttonStyle(.plain)
+                    .padding(7)
+                    .accessibilityLabel("全屏查看视频")
+                }
             } else {
                 Text(message.kind == "attachment_deleted" ? "附件已由管理员删除" : message.content)
                     .textSelection(.enabled).padding(.horizontal,13).padding(.vertical,10)
@@ -267,9 +298,116 @@
                     .foregroundStyle(isAgent ? Color.white : (appearance == "dark" ? Color.white : Color.black))
                     .clipShape(RoundedRectangle(cornerRadius:11,style:.continuous))
             }
-        }.frame(maxWidth:290,alignment:isAgent ? .trailing:.leading)
+        }
+        .frame(maxWidth:290,alignment:isAgent ? .trailing:.leading)
+        .fullScreenCover(item:$preview) { item in
+            AppMediaViewer(preview:item)
+        }
+    }
+
+    private func absoluteURL(_ value:String)->URL? {
+        if value.hasPrefix("http://")||value.hasPrefix("https://"){return URL(string:value)}
+        return URL(string:vm.server+value)
+    }
+}
+
+struct AppMediaViewer: View {
+    @Environment(\.dismiss) private var dismiss
+    let preview: MediaPreview
+    @State private var player: AVPlayer
+
+    init(preview: MediaPreview) {
+        self.preview = preview
+        _player = State(initialValue: AVPlayer(url: preview.url))
+    }
+
+    var body: some View {
+        ZStack(alignment:.topTrailing) {
+            Color.black.ignoresSafeArea()
+            if preview.kind == "image" {
+                RemoteZoomableImage(url:preview.url)
+                    .ignoresSafeArea(edges:.bottom)
+            } else {
+                VideoPlayer(player:player)
+                    .frame(maxWidth:.infinity,maxHeight:.infinity)
+                    .background(Color.black)
+                    .onAppear { player.play() }
+            }
+            Button { dismiss() } label: {
+                Image(systemName:"xmark")
+                    .font(.system(size:18,weight:.bold))
+                    .foregroundStyle(.white)
+                    .frame(width:44,height:44)
+                    .background(.black.opacity(0.58),in:Circle())
+            }
+            .buttonStyle(.plain)
+            .padding(.top,12)
+            .padding(.trailing,16)
+            .accessibilityLabel("关闭预览")
+        }
+        .statusBarHidden(true)
+        .onDisappear { player.pause() }
+    }
+}
+
+struct RemoteZoomableImage: View {
+    let url: URL
+    var body: some View {
+        AsyncImage(url:url) { phase in
+            switch phase {
+            case .success(let image):
+                ZoomableImage(image:image)
+            case .failure:
+                VStack(spacing:12) {
+                    Image(systemName:"exclamationmark.triangle").font(.largeTitle)
+                    Text("图片加载失败")
+                }.foregroundStyle(.white.opacity(0.8))
+            default:
+                ProgressView().tint(.white).scaleEffect(1.2)
+            }
+        }
+        .frame(maxWidth:.infinity,maxHeight:.infinity)
+    }
+}
+
+struct ZoomableImage: View {
+    let image: Image
+    @State private var scale: CGFloat = 1
+    @State private var baseScale: CGFloat = 1
+    @State private var offset: CGSize = .zero
+    @State private var baseOffset: CGSize = .zero
+
+    var body: some View {
+        image
+            .resizable()
+            .scaledToFit()
+            .scaleEffect(scale)
+            .offset(offset)
+            .frame(maxWidth:.infinity,maxHeight:.infinity)
+            .contentShape(Rectangle())
+            .gesture(
+                MagnificationGesture()
+                    .onChanged { value in scale = min(max(baseScale * value, 1), 5) }
+                    .onEnded { _ in
+                        baseScale = scale
+                        if scale <= 1 { scale = 1; baseScale = 1; offset = .zero; baseOffset = .zero }
+                    }
+            )
+            .simultaneousGesture(
+                DragGesture()
+                    .onChanged { value in
+                        guard scale > 1 else { return }
+                        offset = CGSize(width:baseOffset.width + value.translation.width, height:baseOffset.height + value.translation.height)
+                    }
+                    .onEnded { _ in baseOffset = offset }
+            )
+            .onTapGesture(count:2) {
+                withAnimation(.easeInOut(duration:0.2)) {
+                    if scale > 1 { scale = 1; baseScale = 1; offset = .zero; baseOffset = .zero }
+                    else { scale = 2.4; baseScale = 2.4 }
+                }
+            }
     }
-    private func absoluteURL(_ value:String)->URL? { if value.hasPrefix("http://")||value.hasPrefix("https://"){return URL(string:value)};return URL(string:vm.server+value) }
 }
 
 struct VisitorAvatar:View { var body:some View{Circle().fill(LinearGradient(colors:[.cyan,.blue],startPoint:.topLeading,endPoint:.bottomTrailing)).frame(width:38,height:38).overlay(Text("访").font(.caption.bold()).foregroundStyle(.white))} }
--- a/project.yml
+++ b/project.yml
@@ -22,7 +22,7 @@
         TARGETED_DEVICE_FAMILY: "1"
         ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
         MARKETING_VERSION: 1.0.4
-        CURRENT_PROJECT_VERSION: 7
+        CURRENT_PROJECT_VERSION: 8
     entitlements:
       path: CustomerServiceApp/CustomerServiceApp.entitlements
       properties:
